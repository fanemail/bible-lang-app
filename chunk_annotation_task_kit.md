# 語塊標注子任務包（獨立使用，不需上傳其他專案文件）v2.2

> 這份文件是自包含的，適用於**任何書卷**（詩篇、箴言、馬太福音…）。開新對話窗口時，
> **只需上傳這份文件 + 你的經文文字範圍**，不需要上傳 bible_lang_app_project.md、
> handoff_note.md、MASTER_BLUEPRINT.md 等其他專案文件。
> AI 不需要知道整個專案的來龍去脈，只需要知道怎麼標注、標成什麼格式。

---

## 一句話任務說明

給你一段英文聖經經文（逐節，可能是詩篇、箴言、福音書或其他任何書卷），請你依照下方規則，
找出裡面的固定語塊（idiom / phrasal verb / 固定動賓搭配），標注成指定的JSON格式。
**只處理英文**，中文/日文語塊標注留待後續另一個子任務處理，這次不用管。

---

## 一、語塊的定義（先判斷「這是不是語塊」，再談標注格式）

### 內涵（本質特徵）——同時符合以下三點才算語塊

1. **語意不透明或固化（Semantic Non-compositionality）**：整體意思不能單純把每個字的字面意思加總得出。從完全不透明（`kick the bucket`）到部分透明（`white lie`）都算，透明度高低只影響判定的信心分數，不影響「算不算」。

2. **形式固定或半固定（Formal Fixedness）**：詞語之間有慣例化的固定搭配關係，容許有限度的語法變化（時態、單複數、短距插入1-2個修飾詞），但**不能自由替換核心詞**。例：`lay out plans` / `laid out plans`（時態變化，仍算同一語塊）；但核心詞被換成別的詞，就不算了。

3. **整體檢索性（Unitary Storage and Retrieval）**：母語者使用時，把這個詞組當成一個單位從記憶裡整串取出，而不是每次臨時組裝。**測試方法**：這個詞組能不能找到一個單詞或另一個固定詞組去替換、同時保留原意？（例：`put up with` = `tolerate`）能替換是強力證據；即使沒有對應單詞，只要是母語者公認的固定講法、不是臨時拼湊，也算數。

### 外延（範圍界線）

**算的：**
- 成語/習語（idiom），不透明度高低都算
- 動詞片語（phrasal verb）：`give up`、`look forward to`
- 固定動詞-受詞搭配（VNC）：`make a mistake`、`lose one's temper`——**判斷方法見下方「VNC替換測試法」**
- 固定介詞/副詞短語：`by and large`、`on the other hand`
- 日文慣用句（留待B2子任務處理，這份任務包不涉及）

**不算的：**
- 完全自由組合：`eat an apple`（核心詞可任意替換，不具固化搭配關係）
- 純語法性質字串，本身無獨立意義：`of the`、`in a`
- 單一個詞（不管多罕見或多有比喻義，屬於詞典範疇，不是語塊）
- 完整諺語/格言（見下方邊界規則1）
- 已經死掉的古語表達（見下方Step 3現代適用度篩選）

---

## 二、三條邊界規則（機械式判斷，不用AI猜，直接排除）

### 邊界規則1：長度上限 + 排除諺語
- **字數上限：6個實詞以內**（不含 `or`、`someone's` 這類選項/佔位詞）
- **不能是一個獨立完整句子**（有自己的主詞+動詞、能單獨成立表達一個完整意思的，就是諺語不是語塊，直接排除，不用另外標注諺語庫）

### 邊界規則2：排除含人名地名的結構
候選語塊裡任何一個詞如果是聖經人名/地名（對照 `proper_nouns.json`，2906筆），直接排除，**不用送進AI語意判斷**，機械比對即可篩掉。
注意：精確定義是「候選詞組裡含有proper_nouns.json清單裡的詞」，不是「含大寫字母開頭的詞就排除」，避免以後誤傷其他有學習價值的表達。

### 邊界規則3（適用於VNC類型）：替換測試法
判斷一個「動詞+受詞」組合是不是固定搭配，用這個具體測試，不要純憑語感：
> 把動詞換成一個意思相近的同義詞，母語者聽起來還自然嗎？
> - `make a decision` → `*do a decision`（不自然，不能換）→ **是固定搭配，收進來**
> - `eat an apple` → `consume an apple`（換了還是自然）→ **不是固定搭配，排除**

這條規則配合Step 1的信心分級一起用：AI用替換測試法輔助判斷，>70%信心才收（判定標準跟其他語塊一致，只是VNC類型多了這個具體可操作的測試方法，避免AI過度寬鬆地把任何動詞+受詞都當成固定搭配）。

---

## 三、判定流程（三層，含信心分級）

```
Step 1：判斷語義固化程度（是否符合上方「內涵三特徵」；VNC類型另外套用替換測試法）
        >70%信心 → 進入Step 2
        50-70%信心 → 這詞組本身值得注意，但信心不夠，不標注（跳過）
        <50%信心 → 不標注（跳過）
        【六庫命中可跳過本步驟，見下方「四、六庫與AI判斷的關係」】

Step 2：與六個標準庫比對（彈性容忍：時態/語態/短距插入詞變化都算命中）
  IS = COCA五文類最常用成語（250條，已就緒）
  IC = 牛津成語詞典主題索引（1506條，目前僅60條範例，待作者回信補齊）
  VS = PHRASE List核心片語（505條，待找補充資料）
  VC = VNC-Tokens Dataset（2984條，待作者回信）
  JS = Wiktionary日語慣用句（312條，日文專用，這份任務不涉及）
  JC = 已排除（版權）
  US = 使用者自訂庫（待整理，尚未啟用）
  命中任一庫 → 標對應代碼，不額外標AI
  六庫皆未收錄（且不是表層變化） → 標 AI()（信心需>70%）

Step 3：現代適用度篩選（不管Step 2結果如何，一律要過這關）
  如果AI判斷這個語塊「現代適用度」低——即這個表達是聖經古典書面語境特有的，
  現代口語和日常英語幾乎不使用——則放棄不標注，即使語義固化程度符合Step1門檻也不標。
  目的：確保標注出來的語塊對學習者有實際的現代英語使用價值，不只是聖經文學知識。
```

**彈性比對原則**：短語比對不是單詞比對。容忍時態變化、語態轉換、短距插入（1-2詞修飾語）；不容忍核心詞被替換成完全不同的詞、插入過長（3-4詞以上）、詞序顛倒改變語法關係。

**重疊規則**：同一詞條同時在S庫（較小庫）和C庫（較大庫）出現，唯一標注為S（規模小=更核心）。

---

## 四、六庫與AI判斷的關係（重要，先理解這個分工再開始標注）

**六庫是「保證命中即標注」的最小固定子集，範圍以外全部靠AI自主判斷。這是兩個獨立維度：**

- **六庫命中**：跳過Step 1（語意固化度判斷），因為能收進學術辭典/資料集的，已經是語言學者驗證過的固定搭配，不需要AI再猜一次「這算不算語塊」
- **六庫沒收錄**：走完整的Step 1信心分級判斷
- **不管六庫命中與否，Step 3（現代適用度篩選）+ 三條邊界規則都必須過**——六庫收錄的東西不保證每條在現代英語裡都還活著（IC那份研究自己就發現，1506條裡有234條在COCA語料庫出現頻率是零），六庫命中省下的只是「算不算語塊」這一步的判斷成本，不是品質把關的豁免權

**六庫是動態、獨立於標注進度的維度**：
- 現在六庫裡只有IS(250條)真正就緒，IC/VS/VC都還在等（作者回信/找資料），這**不影響現在就開始B1標注**——絕大部分候選語塊本來就會走AI自主判斷這條路，六庫只是輔助分類，不是標注的前提條件
- 之後IC/VC/VS陸續到位，會用一支獨立的「補標腳本」回頭比對所有標`AI()`的語塊，命中的話**只改`tag`欄位**（從`AI()`換成對應代碼），`gloss`/`blank`/`answer`/`ref`/`usage`這些AI當初已經判斷好的內容完全不動，不需要重新標注、不需要重新想gloss
- 這支補標腳本要對著各批次的原始檔案（`chunks_en_{書卷代碼}_*.json`）做修改，不是對著`merge_chunks.py`合併出來的`chunks_data.js`改，因為後者每次重新合併都會被覆蓋重生

**簡單說：現在六庫小，不用等，直接開始標注；以後六庫變大，靠機械補標追上，不用重新判斷語意。**

---

## 五、輸出格式（每個語塊一個JSON物件，欄位缺一不可）

```json
{
  "id": "CK-EN-PSA004-01",
  "chapter": 4,
  "verse": 1,
  "text": "call upon",
  "blank": "call ___ me when I call",
  "answer": "upon",
  "tag": "VS",
  "freq": "medium",
  "gloss": "呼求祈禱",
  "ref": "詩篇 4:1",
  "usage": "現代英語常用於呼求、祈求他人或神明幫助的語境"
}
```
（以上範例取自詩篇，套用到其他書卷時把 `PSA`／`詩篇` 換成該書卷代碼與中文書名即可，見下方各欄位規則。）

**各欄位規則：**

- `id`：格式固定為 `CK-EN-{書卷代碼}{章號3位數}-{該章內第幾個語塊，2位數}`。書卷代碼用 `bible_text/` 資料夾裡該書卷JSON檔的檔名（例如詩篇是`PSA`、箴言是`PRO`、馬太福音是`MAT`），本次任務實際的書卷代碼請看下方「七、這次任務的具體範圍」。例如箴言3章第1個語塊是 `CK-EN-PRO003-01`，第2個是 `CK-EN-PRO003-02`。**不要自己編號規則，一定要用這個格式**，這樣不同窗口分頭做也不會撞號，也不會跟B2（日文，`CK-JA-`前綴）衝突。
- `chapter` / `verse`：純數字，這個語塊出現在第幾章第幾節。
- `text`：語塊在經文中的原始寫法（照抄，不要改寫）。
- `blank`：填空測驗用的句子模板，把 `answer` 對應的那個字換成 `___`。挖空哪個字由你判斷——通常挖最關鍵、最能測試學習者是否掌握這個搭配的那個詞。
- `answer`：`blank` 裡 `___` 對應的正確答案（單字）。
- `tag`：`IS` / `IC` / `VS` / `VC` / `AI()` 五選一（依判定流程Step2結果，JS/JC不適用於這份英文任務）。
- `freq`：`high` / `medium` 二選一，標記這個語塊在**現代英語**裡的使用頻率主觀判斷（不是語料庫詞頻，是你自己的語感判斷，屬於過渡性欄位）。`high` = 日常/新聞/口語中很常見、母語者一看就懂（例：give thanks, turn back）；`medium` = 存在且可理解，但較正式、較少見、或偏書面語（例：have dominion over, land of the living）。不確定時預設 `medium`，不要為了湊數硬標`high`。
- `gloss`：**4-8字中文關鍵詞**，只寫「是什麼」，不寫「為什麼/如何」。
  例：「持續困擾」✓，「用具體場景畫面承載抽象困境」✗。
- `ref`：固定格式 `{該書卷中文名} {章}:{節}`，例如詩篇是 `詩篇 4:1`、箴言是 `箴言 3:5`、馬太福音是 `馬太福音 5:3`。中文書名請看下方「七、這次任務的具體範圍」裡標明的書名，或沿用經文文字檔案裡 `=== {書卷} {中文書名} {章} ===` 這行標示的中文名稱。
- `usage`：一句話說明這個語塊在**現代英語**裡怎麼用、什麼語境下用，20-40字左右，不用太長。

---

## 六、交付格式

把這次處理的所有語塊，輸出成**一個JSON陣列**，存成一個檔案，檔名格式：
`chunks_en_{書卷代碼}_{起始章:03d}-{結束章:03d}.json`

例如處理箴言4~10章，檔名就是 `chunks_en_PRO_004-010.json`；處理詩篇4~10章，
沿用既有慣例 `chunks_en_psalms_004-010.json`（詩篇是歷史包袱，批次檔名固定用小寫`psalms`，
其餘書卷一律用書卷代碼，如`PRO`、`MAT`）。內容像這樣：

```json
[
  { "id":"CK-EN-PRO004-01", "chapter":4, "verse":1, "text":"...", ... },
  { "id":"CK-EN-PRO004-02", "chapter":4, "verse":4, "text":"...", ... },
  { "id":"CK-EN-PRO005-01", "chapter":5, "verse":3, "text":"...", ... }
]
```

---

## 七、這次任務的具體範圍（每次開新窗口前，把這段改成實際要做的書卷與章節）

```
本次任務書卷：___（書卷代碼，例如 PSA / PRO / MAT）
中文書名：___（例如 詩篇 / 箴言 / 馬太福音）
本次任務章節：___ 章 ~ ___ 章
經文文字：（貼上 extract_chapter_text.py 產生的文字）
```

---

## 八、不需要做的事（避免超出範圍）

- 不用標注中文、日文語塊（那是另一個子任務）
- 不用管詞典（vocab_db）、音頻、密度統計、UI這些，都跟這個任務無關
- 不用重新討論標注規則本身，上面的規則是定案的，直接套用
- 不確定某個語塊算不算數時，寧可保守不標，不要為了湊數硬標
- 不用擔心六庫目前不齊全——六庫是輔助分類，不是標注的前提，該標的照標，之後會有補標機制自動追上

---

*子任務包版本：v2.2*
*對應主專案：bible_lang_app_project.md v6.0 語塊標注章節 + MASTER_BLUEPRINT.md*
*本版更新：通用化書卷代碼/中文書名（id、ref、交付檔名不再寫死詩篇），支援詩篇以外的任何書卷*
