# 日文語塊標注子任務包（獨立使用，不需上傳其他專案文件）v1.2

> 這份文件是自包含的，適用於**任何書卷**（詩篇、箴言、馬太福音…）。開新對話窗口時，
> **只需上傳這份文件 + 你的日文經文文字範圍**，不需要上傳其他專案文件。
> AI 不需要知道整個專案的來龍去脈，只需要知道怎麼標注、標成什麼格式。
>
> 本任務包對應英文語塊標注任務包（B1, v2.2）的姐妹篇，判定邏輯架構相同，
> 但語塊定義、邊界規則細節依日文語言學特性調整。

---

## 一句話任務說明

給你一段日文聖經經文（口語訳，逐節，可能是詩篇、箴言、福音書或其他任何書卷），
請你依照下方規則，找出裡面的固定語塊（慣用句 / 複合動詞 / 固定助詞搭配），
標注成指定的JSON格式。**只處理日文**，標注對象是給中文母語、正在學習日文的使用者看的。

---

## 一、語塊的定義（先判斷「這是不是語塊」，再談標注格式）

### 內涵（本質特徵）——同時符合以下三點才算語塊

1. **意味的非構成性（語意不透明或固化）**：整體意思不能單純把每個字/語素的字面意思加總得出。
   從完全不透明（`油を売る`＝閒聊摸魚，跟油完全無關）到部分透明（`実を結ぶ`＝結出成果，
   字面「結果實」延伸出「努力有成」）都算，透明度高低只影響判定的信心分數，不影響「算不算」。

2. **形式固定或半固定**：詞語之間有慣例化的固定搭配關係，容許有限度的語法變化
   （活用形變化如時態、丁寧體/普通體、可能形；短距插入1-2個助詞或修飾詞），
   但**不能自由替換核心語素**。例：`実を結ぶ` / `実が結ばれる`（態的變化，仍算同一語塊）；
   但核心詞被換成別的詞（如換成「実を作る」），就不算了。

3. **整體檢索性**：母語者使用時，把這個詞組當成一個單位從記憶裡整串取出，
   而不是每次臨時組裝。**測試方法**：這個詞組能不能找到一個對應的單詞或另一個固定詞組去替換、
   同時保留原意？（例：`昼も夜も` ≈ `絶えず`／`常に`）能替換是強力證據；
   即使沒有對應單詞，只要是母語者公認的固定講法、不是臨時拼湊，也算數。

### 外延（範圍界線）

**算的：**
- 慣用句（例：`実を結ぶ`、`目に留まる`、`心を込める`）
- 複合動詞中已固化為比喻義的（例：`立ち上がる`用於「起義／振作」的比喻義，
  而非單純「站起來」的字面動作時才算；純字面動作的複合動詞不算，見下方邊界規則）
- 固定助詞搭配 / 慣用表現（例：`〜にあたって`、`〜を込めて`、`〜のもとに`）
- 固定副詞性表現（例：`昼も夜も`、`朝な夕な`——需另過現代適用度篩選）
- 諺語以外的固定比喻表現

**不算的：**
- 完全自由組合（例：`りんごを食べる`，核心詞可任意替換）
- 純語法性質字串，本身無獨立意義（例：`〜のに`、`〜という`單純接續用法）
- 單一個詞（不管多罕見或多有比喻義，屬於詞典範疇，不是語塊）
- 完整的諺語/格言（見下方邊界規則1）
- 已死掉的文語/古語表達，現代日文完全不用的（見下方Step 3現代適用度篩選——
  口語訳本身文體偏文語調，這條特別重要）

---

## 二、三條邊界規則（機械式判斷，不用AI猜，直接排除）

### 邊界規則1：長度上限 + 排除諺語
- **字數上限：實質語素4個以內**（不含助詞本身，但固定搭配中的助詞算在語塊內，
  例如`実を結ぶ`＝実／を／結ぶ，實質語素是「実」「結ぶ」2個，在上限內）
- **不能是一個獨立完整句子**（有自己的主語+述語、能單獨成立表達一個完整意思的，
  是諺語不是語塊，直接排除，不用另外標注諺語庫）

### 邊界規則2：排除含人名地名的結構
候選語塊裡任何一個詞如果是聖經人名/地名（對照 `proper_nouns.json`），
直接排除，**不用送進AI語意判斷**，機械比對即可篩掉。

### 邊界規則3（適用於複合動詞、動詞+助詞搭配）：替換測試法
判斷一個「動詞為核心」的組合是不是固定搭配，用這個具體測試：
> 把動詞換成一個意思相近的同義詞，母語者聽起來還自然嗎？
> - `実を結ぶ` → `*実を作る`（不自然，不能換）→ **是固定搭配，收進來**
> - `りんごを食べる` → `りんごを口にする`（換了還是自然，只是文體差異）→ **不是固定搭配，排除**

這條規則配合Step 1的信心分級一起用，避免AI過度寬鬆地把任何動詞+目的語都當成固定搭配。

---

## 三、判定流程（三層，含信心分級）

```
Step 1：判斷語義固化程度（是否符合上方「內涵三特徵」；複合動詞、動詞+助詞另外套用替換測試法）
        >70%信心 → 進入Step 2
        50-70%信心 → 這詞組本身值得注意，但信心不夠，不標注（跳過）
        <50%信心 → 不標注（跳過）
        【六庫命中可跳過本步驟，見下方「四、六庫與AI判斷的關係」】

Step 2：與標準庫比對（彈性容忍：活用形變化、丁寧體/普通體、短距插入詞變化都算命中）
  JS = Wiktionary日語慣用句庫（312條，已就緒）
  JC = 已排除（版權）
  US = 使用者自訂庫（待整理，尚未啟用）
  命中任一庫 → 標對應代碼，不額外標AI
  庫皆未收錄（且不是表層變化） → 標 AI()（信心需>70%）

Step 3：現代適用度篩選（不管Step 2結果如何，一律要過這關）
  如果AI判斷這個語塊「現代適用度」低——即這個表達是口語訳特有的文語調、
  聖經古典書面語境特有的，現代日常日語幾乎不使用——則放棄不標注，
  即使語義固化程度符合Step1門檻也不標。
  這條對日文任務特別重要：口語訳雖然名為「口語」，實際上仍帶有明治/大正時期
  翻譯腔的文語尾（如「〜である」「〜べし」「〜ならん」），這類文體特徵本身
  不是語塊，也不該被當成語塊標注；只挑會出現在現代日常日語、新聞、對話中的表達。
  目的：確保標注出來的語塊對學習者有實際的現代日語使用價值，不只是古典文語知識。
```

**彈性比對原則**：短語比對不是單詞比對。容忍活用形變化、態的轉換、短距插入
（1個助詞或修飾語）；不容忍核心語素被替換成完全不同的詞、插入過長、語序顛倒改變語法關係。

**重疊規則**：同一詞條同時符合固定表現與複合動詞判斷時，優先標為語意較不透明的判定（通常是慣用句JS優先於單純複合動詞）。

---

## 四、六庫與AI判斷的關係

**JS庫是「保證命中即標注」的固定子集，範圍以外全部靠AI自主判斷：**

- **JS庫命中**：跳過Step 1，因為能收進Wiktionary慣用句分類的，已經是詞典驗證過的固定搭配
- **JS庫沒收錄**：走完整的Step 1信心分級判斷
- **不管JS庫命中與否，Step 3（現代適用度篩選）+ 三條邊界規則都必須過**
- JS庫目前312條規模不大，這**不影響現在就開始B2標注**——絕大部分候選語塊本來就會走AI自主判斷這條路
- 之後若JS庫擴充，用獨立的補標腳本回頭比對所有標`AI()`的語塊，只改`tag`欄位，
  不動`gloss`/`blank`/`answer`/`ref`/`usage`

---

## 五、輸出格式（每個語塊一個JSON物件，欄位缺一不可）

```json
{
  "id": "CK-JA-PSA001-01",
  "chapter": 1,
  "verse": 3,
  "text": "実を結び",
  "reading": "みをむすび",
  "blank": "時が来ると実を___、その葉もしぼまない",
  "answer": "結び",
  "tag": "AI()",
  "freq": "high",
  "gloss": "開花結果、有成效",
  "ref": "詩篇 1:3",
  "usage": "現代日文常用於形容努力後獲得成果，如「努力が実を結ぶ」"
}
```
（以上範例取自詩篇，套用到其他書卷時把 `PSA`／`詩篇` 換成該書卷代碼與中文書名即可，見下方各欄位規則。）

**各欄位規則：**

- `id`：格式固定為 `CK-JA-{書卷代碼}{章號3位數}-{該章內第幾個語塊，2位數}`。書卷代碼用 `bible_text/` 資料夾裡該書卷JSON檔的檔名（例如詩篇是`PSA`、箴言是`PRO`、馬太福音是`MAT`），本次任務實際的書卷代碼請看下方「七、這次任務的具體範圍」。例如箴言3章第1個語塊是 `CK-JA-PRO003-01`。**不要自己編號規則**，這樣不同窗口分頭做也不會撞號，也跟B1（英文，`CK-EN-`前綴）不會衝突。
- `chapter` / `verse`：純數字，這個語塊出現在第幾章第幾節。
- `text`：語塊在經文中的原始寫法（照抄，包含實際出現的活用形，不要還原成辭書形）。
- `reading`：整個`text`的平假名讀音（比照vocab_db.jsonl用pykakasi產生讀音的既有慣例）。
- `blank`：填空測驗用的句子模板，把 `answer` 對應的部分換成 `___`。
  挖空哪個部分由你判斷——通常挖最關鍵、最能測試學習者是否掌握這個搭配的動詞或助詞部分。
- `answer`：`blank` 裡 `___` 對應的正確答案。
- `tag`：`JS` / `US` / `AI()` 三選一（依判定流程Step2結果）。
- `freq`：`high` / `medium` 二選一，標記這個語塊在**現代日文**裡的使用頻率主觀判斷（不是語料庫詞頻，是你自己的語感判斷，屬於過渡性欄位）。`high` = 日常對話、新聞、書寫中很常見，母語者一看就懂（例：実を結ぶ、立ちあがる）；`medium` = 存在且可理解，但較正式、較少見、或偏書面語（例：帰せよ、こうむって）。不確定時預設 `medium`，不要為了湊數硬標`high`。
- `gloss`：**4-8字中文關鍵詞**，只寫「是什麼」，不寫「為什麼/如何」。
  例：「開花結果、有成效」✓，「用具體的植物生長畫面來比喻抽象的人生努力歷程」✗。
- `ref`：固定格式 `{該書卷中文名} {章}:{節}`，例如詩篇是 `詩篇 1:3`、箴言是 `箴言 3:5`、馬太福音是 `馬太福音 5:3`。中文書名請看下方「七、這次任務的具體範圍」裡標明的書名，或沿用經文文字檔案裡 `=== {書卷} {中文書名} {章} ===` 這行標示的中文名稱。
- `usage`：一句話說明這個語塊在**現代日文**裡怎麼用、什麼語境下用（用中文寫），
  20-40字左右，最好附一個現代日文例句片段。

---

## 六、交付格式

把這次處理的所有語塊，輸出成**一個JSON陣列**，存成一個檔案，檔名格式：
`chunks_ja_{書卷代碼}_{起始章:03d}-{結束章:03d}.json`

例如處理箴言4~10章，檔名就是 `chunks_ja_PRO_004-010.json`；處理詩篇4~10章，
沿用既有慣例 `chunks_ja_psalms_004-010.json`（詩篇是歷史包袱，批次檔名固定用小寫`psalms`，
其餘書卷一律用書卷代碼，如`PRO`、`MAT`）。

---

## 七、這次任務的具體範圍（每次開新窗口前，把這段改成實際要做的書卷與章節）

```
本次任務書卷：___（書卷代碼，例如 PSA / PRO / MAT）
中文書名：___（例如 詩篇 / 箴言 / 馬太福音）
本次任務章節：___ 章 ~ ___ 章（日文，口語訳）
經文文字：（貼上擷取出來的日文經文文字）
```

---

## 八、不需要做的事（避免超出範圍）

- 不用標注英文、中文語塊（英文是B1子任務，中文語塊目前尚未規劃獨立子任務）
- 不用管詞典（vocab_db）、音頻、密度統計、UI這些，都跟這個任務無關
- 不用重新討論標注規則本身，上面的規則是定案的，直接套用
- 不確定某個語塊算不算數時，寧可保守不標，不要為了湊數硬標
- 不用擔心JS庫目前不齊全——JS庫是輔助分類，不是標注的前提，該標的照標
- **特別注意**：口語訳文體偏文語調，不要把單純的文語語尾/古典助動詞
  （如「〜である」「〜べし」「〜ならん」「〜たまう」）誤判為語塊，
  這些是文體特徵不是固定搭配語塊，一律不標

---

*子任務包版本：v1.2*
*對應英文姐妹篇：chunk_annotation_task_kit.md v2.2*
*對應主專案：bible_lang_app_project.md + MASTER_BLUEPRINT.md B2任務*
*本版更新：通用化書卷代碼/中文書名（id、ref、交付檔名不再寫死詩篇），支援詩篇以外的任何書卷*
